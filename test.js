/** delete duplicate items of a array
 **/
const fruits = ['banana', 'apple', 'pear', 'watermelon', 'orange', 'apple']

// const uniqueFruits = Array.from(new Set(fruits))
const uniqueFruits = [...new Set(fruits)]

/** delete duplicate items of a array
 **/

const x = new Array(5).fill(1)
x.length = 0
x.length = 3

const temp = undefined
console.log(typeof temp)

